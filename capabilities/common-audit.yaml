naftiko: 1.0.0-alpha2
info:
  label: Oracle Fusion Cloud Applications Oracle Fusion Common Features REST API — Audit
  description: 'Oracle Fusion Cloud Applications Oracle Fusion Common Features REST API — Audit. 1 operations. Lead operation:
    Oracle Fusion Cloud Applications Get audit history. Self-contained Naftiko capability covering one Oracle Fusion business
    surface.'
  tags:
  - Oracle Fusion
  - Audit
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ORACLE_FUSION_API_KEY: ORACLE_FUSION_API_KEY
capability:
  consumes:
  - type: http
    namespace: common-audit
    baseUri: https://{instance}.oraclecloud.com
    description: Oracle Fusion Cloud Applications Oracle Fusion Common Features REST API — Audit business capability. Self-contained,
      no shared references.
    resources:
    - name: fscmRestApi-fndAuditRESTService-audittrail-getaudithistory
      path: /fscmRestApi/fndAuditRESTService/audittrail/getaudithistory
      operations:
      - name: getaudithistory
        method: GET
        description: Oracle Fusion Cloud Applications Get audit history
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.ORACLE_FUSION_API_KEY}}'
  exposes:
  - type: rest
    namespace: common-audit-rest
    port: 8080
    description: REST adapter for Oracle Fusion Cloud Applications Oracle Fusion Common Features REST API — Audit. One Spectral-compliant
      resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/fscmrestapi/fndauditrestservice/audittrail/getaudithistory
      name: fscmrestapi-fndauditrestservice-audittrail-getaudithistory
      description: REST surface for fscmRestApi-fndAuditRESTService-audittrail-getaudithistory.
      operations:
      - method: GET
        name: getaudithistory
        description: Oracle Fusion Cloud Applications Get audit history
        call: common-audit.getaudithistory
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: common-audit-mcp
    port: 9090
    transport: http
    description: MCP adapter for Oracle Fusion Cloud Applications Oracle Fusion Common Features REST API — Audit. One tool
      per consumed operation, routed inline through this capability's consumes block.
    tools:
    - name: oracle-fusion-cloud-applications-get
      description: Oracle Fusion Cloud Applications Get audit history
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: common-audit.getaudithistory
      outputParameters:
      - type: object
        mapping: $.
