naftiko: 1.0.0-alpha2
info:
  label: Oracle Fusion Cloud Applications Oracle Fusion Project Management REST API — Project Tasks
  description: 'Oracle Fusion Cloud Applications Oracle Fusion Project Management REST API — Project Tasks. 2 operations.
    Lead operation: Oracle Fusion Cloud Applications List project tasks. Self-contained Naftiko capability covering one Oracle
    Fusion business surface.'
  tags:
  - Oracle Fusion
  - Project Tasks
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ORACLE_FUSION_API_KEY: ORACLE_FUSION_API_KEY
capability:
  consumes:
  - type: http
    namespace: project-management-project-tasks
    baseUri: https://{instance}.oraclecloud.com
    description: Oracle Fusion Cloud Applications Oracle Fusion Project Management REST API — Project Tasks business capability.
      Self-contained, no shared references.
    resources:
    - name: fscmRestApi-resources-11.13.18.05-projectTasks
      path: /fscmRestApi/resources/11.13.18.05/projectTasks
      operations:
      - name: listprojecttasks
        method: GET
        description: Oracle Fusion Cloud Applications List project tasks
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: createprojecttask
        method: POST
        description: Oracle Fusion Cloud Applications Create a project task
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.ORACLE_FUSION_API_KEY}}'
  exposes:
  - type: rest
    namespace: project-management-project-tasks-rest
    port: 8080
    description: REST adapter for Oracle Fusion Cloud Applications Oracle Fusion Project Management REST API — Project Tasks.
      One Spectral-compliant resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/fscmrestapi/resources/11-13-18-05/projecttasks
      name: fscmrestapi-resources-11-13-18-05-projecttasks
      description: REST surface for fscmRestApi-resources-11.13.18.05-projectTasks.
      operations:
      - method: GET
        name: listprojecttasks
        description: Oracle Fusion Cloud Applications List project tasks
        call: project-management-project-tasks.listprojecttasks
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: createprojecttask
        description: Oracle Fusion Cloud Applications Create a project task
        call: project-management-project-tasks.createprojecttask
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: project-management-project-tasks-mcp
    port: 9090
    transport: http
    description: MCP adapter for Oracle Fusion Cloud Applications Oracle Fusion Project Management REST API — Project Tasks.
      One tool per consumed operation, routed inline through this capability's consumes block.
    tools:
    - name: oracle-fusion-cloud-applications-list
      description: Oracle Fusion Cloud Applications List project tasks
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: project-management-project-tasks.listprojecttasks
      outputParameters:
      - type: object
        mapping: $.
    - name: oracle-fusion-cloud-applications-create
      description: Oracle Fusion Cloud Applications Create a project task
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: project-management-project-tasks.createprojecttask
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
