naftiko: 1.0.0-alpha2
info:
  label: Oracle Fusion Cloud Applications Oracle Fusion Cloud SCM REST API — Work Orders
  description: 'Oracle Fusion Cloud Applications Oracle Fusion Cloud SCM REST API — Work Orders. 2 operations. Lead operation:
    Oracle Fusion Cloud Applications List work orders. Self-contained Naftiko capability covering one Oracle Fusion business
    surface.'
  tags:
  - Oracle Fusion
  - Work Orders
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ORACLE_FUSION_API_KEY: ORACLE_FUSION_API_KEY
capability:
  consumes:
  - type: http
    namespace: scm-work-orders
    baseUri: https://{instance}.oraclecloud.com
    description: Oracle Fusion Cloud Applications Oracle Fusion Cloud SCM REST API — Work Orders business capability. Self-contained,
      no shared references.
    resources:
    - name: fscmRestApi-resources-11.13.18.05-workOrders
      path: /fscmRestApi/resources/11.13.18.05/workOrders
      operations:
      - name: listworkorders
        method: GET
        description: Oracle Fusion Cloud Applications List work orders
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: createworkorder
        method: POST
        description: Oracle Fusion Cloud Applications Create a work order
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.ORACLE_FUSION_API_KEY}}'
  exposes:
  - type: rest
    namespace: scm-work-orders-rest
    port: 8080
    description: REST adapter for Oracle Fusion Cloud Applications Oracle Fusion Cloud SCM REST API — Work Orders. One Spectral-compliant
      resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/fscmrestapi/resources/11-13-18-05/workorders
      name: fscmrestapi-resources-11-13-18-05-workorders
      description: REST surface for fscmRestApi-resources-11.13.18.05-workOrders.
      operations:
      - method: GET
        name: listworkorders
        description: Oracle Fusion Cloud Applications List work orders
        call: scm-work-orders.listworkorders
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: createworkorder
        description: Oracle Fusion Cloud Applications Create a work order
        call: scm-work-orders.createworkorder
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: scm-work-orders-mcp
    port: 9090
    transport: http
    description: MCP adapter for Oracle Fusion Cloud Applications Oracle Fusion Cloud SCM REST API — Work Orders. One tool
      per consumed operation, routed inline through this capability's consumes block.
    tools:
    - name: oracle-fusion-cloud-applications-list
      description: Oracle Fusion Cloud Applications List work orders
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: scm-work-orders.listworkorders
      outputParameters:
      - type: object
        mapping: $.
    - name: oracle-fusion-cloud-applications-create
      description: Oracle Fusion Cloud Applications Create a work order
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: scm-work-orders.createworkorder
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
