openapi: 3.2.0
info:
  title: Oracle Fusion Cloud Applications Oracle Fusion Cloud HCM…
  description: REST APIs for Oracle Fusion Cloud Human Capital Management, enabling integration with core HR, talent management, workforce management, and payroll systems. The API provides access to employee records, absence management, benefits, compensation, recruiting, and learning resources.
  version: 11.13.18.05
  contact:
    name: Oracle Support
    url: https://support.oracle.com
    email: support@oracle.com
  license:
    name: Oracle Cloud Services Agreement
    url: https://www.oracle.com/corporate/contracts/cloud-services/
  termsOfService: https://www.oracle.com/corporate/contracts/cloud-services/
servers:
- url: https://{instance}.oraclecloud.com
  description: Oracle Fusion Cloud Instance
  variables:
    instance:
      default: servername
      description: Your Oracle Cloud instance identifier
security:
- bearerAuth: []
tags:
- name: Absences
  description: Absence management and leave tracking
paths:
  /hcmRestApi/resources/11.13.18.05/absences:
    get:
      operationId: listAbsences
      summary: Oracle Fusion Cloud Applications List absences
      description: Retrieve absence records for workers including vacation, sick leave, and other absence types.
      tags:
      - Absences
      parameters:
      - $ref: '#/components/parameters/limit'
      - $ref: '#/components/parameters/offset'
      - $ref: '#/components/parameters/fields'
      - $ref: '#/components/parameters/q'
      - $ref: '#/components/parameters/finder'
      responses:
        '200':
          description: Absences retrieved
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      $ref: '#/components/schemas/Absence'
                  count:
                    type: integer
                  hasMore:
                    type: boolean
        '401':
          $ref: '#/components/responses/Unauthorized'
    post:
      operationId: createAbsence
      summary: Oracle Fusion Cloud Applications Create an absence
      description: Create a new absence record for a worker.
      tags:
      - Absences
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AbsenceCreate'
      responses:
        '201':
          description: Absence created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Absence'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /hcmRestApi/resources/11.13.18.05/absences/{AbsenceId}:
    get:
      operationId: getAbsence
      summary: Oracle Fusion Cloud Applications Get an absence
      description: Retrieve a specific absence record.
      tags:
      - Absences
      parameters:
      - name: AbsenceId
        in: path
        required: true
        description: Unique identifier for the absence
        schema:
          type: integer
      - $ref: '#/components/parameters/fields'
      responses:
        '200':
          description: Absence details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Absence'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      operationId: updateAbsence
      summary: Oracle Fusion Cloud Applications Update an absence
      description: Update an existing absence record.
      tags:
      - Absences
      parameters:
      - name: AbsenceId
        in: path
        required: true
        schema:
          type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AbsenceCreate'
      responses:
        '200':
          description: Absence updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Absence'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    delete:
      operationId: deleteAbsence
      summary: Oracle Fusion Cloud Applications Delete an absence
      description: Delete an absence record.
      tags:
      - Absences
      parameters:
      - name: AbsenceId
        in: path
        required: true
        schema:
          type: integer
      responses:
        '204':
          description: Absence deleted
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
components:
  responses:
    NotFound:
      description: The requested resource was not found
    BadRequest:
      description: Invalid request parameters or payload
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    Unauthorized:
      description: Authentication credentials are missing or invalid
  schemas:
    AbsenceCreate:
      type: object
      required:
      - PersonNumber
      - AbsenceType
      - StartDate
      - EndDate
      properties:
        PersonNumber:
          type: string
        AbsenceType:
          type: string
        AbsenceReason:
          type: string
        StartDate:
          type: string
          format: date
        EndDate:
          type: string
          format: date
        Duration:
          type: number
          format: double
        UnitOfMeasure:
          type: string
          enum:
          - Days
          - Hours
        Comments:
          type: string
    ErrorResponse:
      type: object
      properties:
        type:
          type: string
        title:
          type: string
        detail:
          type: string
        o:errorCode:
          type: string
    Absence:
      type: object
      properties:
        AbsenceId:
          type: integer
          description: Absence record identifier
        PersonNumber:
          type: string
          description: Person number of the worker
        PersonName:
          type: string
          description: Worker display name
        AbsenceType:
          type: string
          description: Type of absence
        AbsenceReason:
          type: string
          description: Reason for absence
        StartDate:
          type: string
          format: date
          description: Absence start date
        EndDate:
          type: string
          format: date
          description: Absence end date
        Duration:
          type: number
          format: double
          description: Duration in days or hours
        UnitOfMeasure:
          type: string
          description: Unit of measure (Days or Hours)
          enum:
          - Days
          - Hours
        ApprovalStatus:
          type: string
          description: Approval status
          enum:
          - Submitted
          - Approved
          - Rejected
          - Saved
          - Withdrawn
        Comments:
          type: string
          description: Comments
  parameters:
    offset:
      name: offset
      in: query
      required: false
      description: Number of items to skip
      schema:
        type: integer
        default: 0
    fields:
      name: fields
      in: query
      required: false
      description: Comma-separated list of fields to return
      schema:
        type: string
    limit:
      name: limit
      in: query
      required: false
      description: Maximum number of items to return (default 25, max 500)
      schema:
        type: integer
        default: 25
        maximum: 500
    q:
      name: q
      in: query
      required: false
      description: Filter expression using Oracle REST query syntax
      schema:
        type: string
    finder:
      name: finder
      in: query
      required: false
      description: Named finder in format FinderName;param1=value1
      schema:
        type: string
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: OAuth 2.0 bearer token or basic authentication
    basicAuth:
      type: http
      scheme: basic
      description: HTTP Basic authentication with Oracle Fusion Cloud credentials
externalDocs:
  description: Oracle Fusion Cloud HCM REST API Documentation
  url: https://docs.oracle.com/en/cloud/saas/human-resources/26a/farws/
